

/* let num = prompt('sayi girin');
try {
    if (num == '') throw 'Boş olamaz'
    if (isNaN(num)) throw 'sayı değil';
    num = Number(num);
} catch (error) {
    alert(error)
} */

//hata tipi
/* let num = 21;
console.log(num.toUpperCase()); */